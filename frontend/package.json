{
  "name": "empleados-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start:watch": "parcel public/index.html --port 3000",
    "build": "parcel build public/index.html --dist-dir dist",
    "test": "jest",
    "cypress:run": "cypress run",
    "cypress:open": "cypress open"
  },
  "dependencies": {
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "parcel": "^2.9.0",
    "jest": "^29.7.0",
    "@testing-library/dom": "^8.0.0",
    "cypress": "^13.6.0"
  }
}
