info:
  name: update
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8080/v1/empleados/E002
  body:
    mode: json
    raw: |
      {
        "clave": "E002",
        "nombre": "Juan Perez",
        "direccion": "Calle 456",
        "telefono": "555-5678",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
