info:
  name: post
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/v1/empleados
  body:
    mode: json
    raw: |
      {
        "clave": "E001",
        "nombre": "Juan Perez",
        "direccion": "Calle 123",
        "telefono": "555-1234",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
