info:
  name: post
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/empleados
  auth: inherit
  body:
    mode: json
    raw: |
      {
        "clave": "E001",
        "nombre": "Juan Perez",
        "puesto": "Desarrollador",
        "salario": 15000
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
