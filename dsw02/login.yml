info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/v1/auth/login
  body:
    mode: json
    raw: |
      {
        "clave": "1",
        "password": "admin123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
